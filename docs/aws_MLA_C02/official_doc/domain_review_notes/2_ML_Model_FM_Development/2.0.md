# Domain 2 Review

## Introduction

??? info "Original video transcript"

    OK, so most engineers have trained a model in a notebook, so model development feels familiar, right? And then a job asks a harder question, not whether you can train a model, whether you can choose the right one and you can prove it works. This domain, ML model and foundation model development covers how you choose, train, and evaluate models from traditional ML to foundation models, and it breaks into three tasks. Let's walk through them. The first is choosing appropriate modeling approaches. That means weighing traditional ML models against foundation models and custom builds against managed options. It also means selecting rag when a use case needs them. The second is training, fine tuning, and customizing models. This is where you can figure training, tune hyperparametters, prevent overfitting, and apply customization techniques like prompt engineering and fine tuning. And the third is analyzing and evaluating performance. You set baselines, detect drift, run reproducible experiments, and evaluate traditional ML models and generative AI outputs with the right metrics. 3 tasks, and they build on each other. The modeling choice shapes the training, and the evaluation tells you whether both actually worked. Skip that last step and you're deploying a guess. So what does this matter for the machine learning engineer? Because these decisions are what separate an engineer from someone who just runs scripts. Exactly. Anyone can start a training job. The engineer knows why this model, why these trade-offs, and how to back the result with evidence instead of a feeling. Make those choices deliberately and you ship models the business can trust. Make them casually and you ship expensive guesses. See, here's how the review works. Each task gets a light pass over the AWS services that relate to it, and then a deeper review of the skills underneath. Some tasks also get a review together video where we work a scenario out loud and the knowledge texts along the way tell you where to slow down. So that's what's covered in this domain choosing, training, and proving models in three connected pieces. Let's get started.

> **Core Principle:** Anyone can launch a training script. An engineer knows *why* a specific model architecture was chosen, understands the architectural trade-offs, and backs model performance with quantitative evidence rather than guesswork.

---

### The 3 Core Tasks

1. **Choosing Appropriate Modeling Approaches**
    * **Traditional ML vs. Foundation Models (FMs):** Selecting tabular/classical algorithms vs. deep learning or GenAI foundation models based on data and task requirements.
    * **Managed Services vs. Custom Builds:** Weighing fully managed options (e.g., Amazon Bedrock, SageMaker JumpStart) against custom training on SageMaker instances.
    * **Architectural Selection:** Identifying when a scenario requires Retrieval-Augmented Generation (RAG) vs. direct inference or fine-tuning.


2. **Training, Customizing, & Fine-Tuning Models**
    * **Training Operations:** Configuring instance types, distributed training, hyperparameter tuning (HPO), and regularizing to prevent overfitting.
    * **GenAI Customization:** Applying prompt engineering techniques first, followed by Parameter-Efficient Fine-Tuning (PEFT/LoRA) or Supervised Fine-Tuning (SFT) when prompting alone is insufficient.


3. **Analyzing & Evaluating Performance**
    * **Baselines & Tracking:** Establishing performance baselines, logging metrics, and running reproducible experiments (e.g., SageMaker Experiments).
    * **Traditional Metrics:** Evaluating tabular models using Precision, Recall, F1, ROC-AUC, RMSE, etc.
    * **GenAI & FM Metrics:** Evaluating foundation model outputs using specialized benchmarks, human-in-the-loop (SageMaker Clarify/Ground Truth), and automated LLM-as-a-judge metrics (e.g., ROUGE, BLEU, semantic similarity).

### Key Pipeline Architectural Takeaway

* **Sequential Dependency Loop:** Domain 2 decisions build directly on one another:

$$\text{Model Choice} \longrightarrow \text{Training / Customization} \longrightarrow \text{Performance Evaluation}$$


* Skipping or rushing evaluation means deploying an unverified guess.
* Evaluation metrics determine whether the modeling approach and training configuration actually succeeded before pushing to deployment.

---

## Bonus Assignment: Choose and defend a modeling approach

This optional, ungraded assignment reviews Domain 2 by taking two requests from the same fictitious company through all three task statements: 

- the approach decision
- the training or customization that follows from it
- the evidence that the result works

### Disclaimer

This project is for educational purposes only and might lead to AWS service charges. You assume any risk associated with this project. You are responsible for any costs associated with your AWS account.

### The scenario
AnyCompany Retail brings you two requests in the same week. Merchandising wants weekly demand forecasts per store, and it has years of its own sales history to work from. Customer service wants an assistant that answers agent questions from a returns policy that legal revises frequently. Nobody has told you which approach either request needs, and that is the assignment.

### Steps
#### Task 2.1: Choose modeling approaches

- Write one sentence per request naming the approach you would take: a rule, a managed AI service, a trained model, or a foundation model, and say what in the request forced that choice.
- For the forecast, name the requirement that constrains the algorithm choice more than accuracy does, and say which algorithm family you would rule out because of it.
- For the assistant, decide between retrieval and customization, then list the parts a production retrieval system needs and mark which ones a managed retrieval service would hand you.
- Name one requirement you would have to give up in each request, choosing from output quality, latency, and cost, and say who in the business absorbs that loss.
#### Task 2.2: Train, fine-tune, and customize models

- For the forecast, decide whether the training code should come from a built-in algorithm, a script of your own on a prebuilt framework container, or a container you build, and justify it in one sentence.
- Pick three hyperparameters you would put into a tuning job, declare a range for each, and name the single objective metric the job optimizes. State whether that metric is minimized or maximized.
- Write the prompt for the assistant with explicit instructions, a stated output format, and two worked examples. Then run it, and note exactly what still falls short.
- Decide from that result whether customization is warranted. If it is, name the technique and the artifact it would require you to prepare. If it is not, say what made prompting sufficient.
#### Task 2.3: Analyze and evaluate performance

- Record the forecast experiment as a run with its parameters, metrics, and artifacts, and write the baseline figure you would compare a later run against.
- Name one metric that would show the forecast model drifting, and say whether it measures the incoming data, the prediction quality, or the endpoint's behavior.
- Evaluate three assistant answers two ways, once against a reference answer and once for whether the retrieved passage actually contained the fact. Note any answer that reads well and still failed.
- Describe how you would compare a revised assistant prompt against the current one without changing what any user receives, and name what you would log to make that comparison honest.
### Check yourself
#### Approach reasoning
Could you defend each approach to somebody who proposed using one foundation model for both requests, using the requirements rather than preference?

#### Prompt before customization
Could you state what specifically failed in your prompt, and why that failure does or does not warrant a customization job?

#### Metric direction
Could you say whether your objective metric is minimized or maximized, and what a tuning job would optimize toward if you pointed it the wrong way?

#### Retrieval versus generation
Could you say which of your assistant's weak answers was a retrieval failure and which was a generation failure, and how you told them apart?

### Find your gap
If any step felt unclear, return to the skills in that task's section before moving on. Note the gap so you know what to review.

### If you have no AWS account
Every step has a paper equivalent. Describe the training route and the tuning ranges instead of submitting a job, write the prompt and predict where it would fall short instead of running it, and reason about which metric would move instead of measuring it. The decisions are the review; an account is only where they become concrete.

### Clean up
Delete anything you created, so nothing is left running or stored: remove any training job output and model artifacts, delete any endpoint or variant you deployed, remove any evaluation job output and the objects it wrote, and delete any prompt or retrieval index you created for the assistant. If you used a generative AI tool to produce sample data, clear its conversation history so the sample data is not retained.